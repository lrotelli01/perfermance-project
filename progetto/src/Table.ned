package progetto;

simple Table
{
    parameters:
        int tableId;    // ID della tabella

    gates:
        input userIn[];    // riceve richieste da tutti gli utenti
        output userOut[];  // invia risposte a tutti gli utenti
}
